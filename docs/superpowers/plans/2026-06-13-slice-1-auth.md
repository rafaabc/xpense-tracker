# Slice 1 — Auth Implementation Plan

**Status:** ✅ Unit + mocked-integration complete (merged, commit `be67097`). E2E deferred (Google OAuth still propagating at time of merge).

**Goal:** Implement and test Google OAuth registration/login, session handling, and route guard.

**User Stories:** US-01 (User Registration with Google), US-02 (User Login with Google)

---

## Test Conditions Coverage

### Unit Tests — `tests/unit/auth/`

| File | TCs Covered | Status |
|------|------------|--------|
| `config.test.ts` | TC-01-08: Only Google provider registered | ✅ |
| `route-guard.test.ts` | TC-02-09: Unauthenticated access redirects to landing | ✅ |

### Mocked Integration Tests — `tests/mocked-integration/auth/`

| File | TCs Covered | Status |
|------|------------|--------|
| `adapter.test.ts` | TC-01-03: createUser called for new identity; TC-01-06: getUserByAccount checked before createUser; TC-01-07: no createUser when identity exists | ✅ |
| `session.test.ts` | TC-02-02: session callback copies user.id from DB into session | ✅ |
| `handler.test.ts` | TC-02-01: GET/POST route handlers bound to NextAuth; TC-02-06: signOut exported and callable; TC-02-07: failed auth → error redirect; TC-02-08: revoked auth → error redirect | ✅ |

### E2E Tests — `tests/e2e/auth/`

| File | TCs Covered | Status |
|------|------------|--------|
| `tests/e2e/auth/landing.test.ts` | TC-01-01: sign-in button visible; TC-01-02: clicking button redirects to Google OAuth | ✅ |
| `tests/e2e/auth/session.test.ts` | TC-01-04: authenticated user lands on dashboard; TC-01-05: dashboard empty for new account; TC-02-04: dashboard shows user's name; TC-02-05: sign-out button visible + redirects to landing | ✅ |
| `tests/e2e/auth/route-guard.test.ts` | TC-02-10: all protected routes redirect unauthenticated users to landing | ✅ |

---

## Implementation Files

| File | Purpose |
|------|---------|
| `lib/auth.ts` | Auth.js v5 — Google provider, DrizzleAdapter, session.user.id callback *(from Slice 0)* |
| `proxy.ts` | Unauthenticated redirect to `/` for all protected routes *(from Slice 0)* |
| `app/api/auth/[...nextauth]/route.ts` | Re-exports `handlers.GET` and `handlers.POST` from `lib/auth` *(from Slice 0)* |

No new implementation files added in Slice 1 — all behavior was already wired in Slice 0; this slice added the tests.

---

## Tasks

- [x] Write `tests/unit/auth/config.test.ts` — TC-01-08
- [x] Run unit test, confirm pass
- [x] Write `tests/unit/auth/route-guard.test.ts` — TC-02-09
- [x] Run unit test, confirm pass
- [x] Write `tests/mocked-integration/auth/adapter.test.ts` — TC-01-03/06/07
- [x] Run mocked-integration test, confirm pass
- [x] Write `tests/mocked-integration/auth/session.test.ts` — TC-02-02
- [x] Run mocked-integration test, confirm pass
- [x] Write `tests/mocked-integration/auth/handler.test.ts` — TC-02-01/06/07/08
- [x] Run all tests (`npm test`), confirm 20 passing
- [x] Fill `.env.local` (Neon DB URL, AUTH_SECRET, Google OAuth credentials)
- [x] Push schema to Neon DB (`npm run db:push`)
- [x] Commit
