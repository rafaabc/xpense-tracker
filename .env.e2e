# E2E test environment overrides (no secrets — safe to commit)
# Fixes SSL cert verification failure when Playwright fixtures connect to Neon
# from the Git Bash / Windows context (uses Windows certificate store instead of OpenSSL bundled certs)
NODE_OPTIONS=--use-system-ca
